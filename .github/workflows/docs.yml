name: Documentation

on:
  push:
    branches: [main]
    paths: ['docs/**', 'README.md', 'assets/**', 'tools/docs/**', 'tools/visuals/**', '.github/workflows/docs.yml']
  pull_request:
    paths: ['docs/**', 'README.md', 'assets/**', 'tools/docs/**', 'tools/visuals/**', '.github/workflows/docs.yml']
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: docs-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v7
        with:
          python-version: '3.12'
      - name: Install uv
        uses: astral-sh/setup-uv@v10.1.0
        with:
          version: '0.9.21'
      - name: Test the exporter
        run: uv run --locked tools/docs/build.py --test
      - name: Build and check links
        run: uv run --locked tools/docs/build.py
      - uses: actions/setup-node@v7
        with:
          node-version: '24'
          cache: npm
          cache-dependency-path: tools/visuals/package-lock.json
      - name: Install visual checks
        working-directory: tools/visuals
        run: |
          npm ci
          npx playwright install --with-deps chromium webkit
      - name: Review the generated site in browsers
        working-directory: tools/visuals
        run: npm test
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: site-review
          path: tools/visuals/artifacts
      - uses: actions/upload-pages-artifact@v4
        with:
          path: _site

  deploy:
    if: github.ref == 'refs/heads/main' && github.event_name != 'pull_request'
    needs: build
    runs-on: ubuntu-24.04
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Configure Pages
        uses: actions/configure-pages@v5
      - name: Publish
        id: deployment
        uses: actions/deploy-pages@v4
