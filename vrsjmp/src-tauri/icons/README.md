Bloom at 0.381 s, after 7 s of idle. Neutral colors, 2× finer grain.

`AppIcon.icon` contains the light and dark artwork. Tauri bundles the compiled
`Assets.car`; `icon.icns` is the fallback for older macOS versions.
